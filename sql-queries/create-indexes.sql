-- CREATE UNIQUE INDEX index_unique_users_login ON users (login);

-- CREATE INDEX index_artist_info_name ON artist_info (name);

-- CREATE INDEX index_user_logs_user_id ON user_logs (user_id);

-- CREATE INDEX index_songs_title ON songs (title);

-- CREATE INDEX index_reviews_song_id ON reviews (song_id);

-- create index index_events_title on events (title);